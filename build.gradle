subprojects {
    apply plugin: 'application'

    version = '0.2'
    group = 'org.bot.reminder'

    repositories {
        mavenCentral()
    }

    dependencies {
        testImplementation 'junit:junit:4.13.2'
    }
}